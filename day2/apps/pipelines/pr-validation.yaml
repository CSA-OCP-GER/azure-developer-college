pr:
  branches:
    include:
      - master
      - features/*
  paths:
    include:
      - day2/apps/dotnetcore/*

stages:
  - stage: Build
    displayName: Build

    jobs:
      - job: Build
        displayName: Build
        pool:
          vmImage: ubuntu-latest
        steps:
          - template: ../dotnetcore/build/scm-api.yaml
